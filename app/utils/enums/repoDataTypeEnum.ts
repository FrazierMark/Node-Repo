export enum RepoDataType {
  // For `Object` node type
  Object = 'object',

  // For `Array` node type
  Array = 'array',

  // For `Primitive` node type
  String = 'string',
  Number = 'number',
  Boolean = 'boolean',
  Null = 'null',
}
